# Copy to .env and fill in real values. Never commit .env.
# Only NEXT_PUBLIC_* variables are exposed to the browser.

# Base URL of the Livora NestJS API
NEXT_PUBLIC_API_URL=http://localhost:4200/api
